#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    int a, b, c, d;

    // Input three integers a, b, c
    cin >> a >> b >> c;

    // Calculate the sum of a, b, and c
    d = a + b + c;

    // Output the sum
    cout << d;

    return 0;
}
